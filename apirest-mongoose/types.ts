//type Person es un alias de un objeto en el que se especifica que tiene que tener los campos name, dni, age e id, todos ellos de un tipo concreto
export type Person = {
  name: string;
  dni: string;
  age: number;
  id: string;
};
